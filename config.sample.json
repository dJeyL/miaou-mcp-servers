{
  "port": 8767,
  "host": "127.0.0.1",
  "mcpServers": {
    "bench": {
      "type": "inprocess",
      "module": "mcp_bench",
      "_comment": "Chargé dans le processus du proxy — aucun subprocess."
    },
    "weather": {
      "type": "inprocess",
      "module": "mcp_weather",
      "_comment": "Chargé dans le processus du proxy — aucun subprocess."
    },
    "web": {
      "_disabled": false,
      "type": "inprocess",
      "module": "mcp_web",
      "_comment": "Télécharge des URLs. Aucun secret requis."
    },
    "duckduckgo": {
      "_disabled": false,
      "type": "inprocess",
      "module": "mcp_ddg",
      "_comment": "Recherche DuckDuckGo (scraping HTML, sans clé API)."
    },
    "brave": {
      "_disabled": true,
      "type": "inprocess",
      "module": "mcp_brave",
      "env": {
        "BRAVE_API_KEY": "your-brave-api-key-here"
      },
      "_comment": "Recherche Brave Search. Remplacer BRAVE_API_KEY puis retirer _disabled."
    },
    "docs": {
      "_disabled": false,
      "type": "inprocess",
      "module": "mcp_docs",
      "_comment": "Extraction PDF/Office/Zip. Cache sur disque dans ./miaou-docs (sous-dossier du répertoire de travail)."
    },
    "_example_stdio": {
      "_disabled": true,
      "_comment": "Exemple de serveur externe lancé en subprocess via stdio.",
      "command": "uv",
      "args": ["run", "servers/mcp_bench.py", "--transport", "stdio"],
      "env": {}
    },
    "_example_multi_prod": {
      "_disabled": true,
      "type": "inprocess",
      "module": "mcp_example_api",
      "config": { "base_url": "https://prod.api.example.com", "client_id": "prod-client-id" },
      "_comment": "Exemple multi-instance : plusieurs entrées du même module, chacune avec sa propre clé 'config' (dict libre lu par le serveur via self.config). Voir aussi _example_multi_uat ci-dessous — même module, config différente. 'env' reste réservé à stdio ou aux serveurs qui lisent réellement os.environ."
    },
    "_example_multi_uat": {
      "_disabled": true,
      "type": "inprocess",
      "module": "mcp_example_api",
      "config": { "base_url": "https://uat.api.example.com", "client_id": "uat-client-id" }
    }
  }
}
