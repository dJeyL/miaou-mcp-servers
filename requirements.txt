# Dépendances runtime — suffisantes pour lancer les trois serveurs.
# Avec uv : uv run <script> installe automatiquement depuis le bloc PEP 723.
# Avec pip : pip install -r requirements.txt

mcp>=1.2
uvicorn
starlette
anyio
html2text

# Dépendances de test
pytest
pytest-asyncio
